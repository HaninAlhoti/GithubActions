name: output github context info.

on: push

jobs:
    info:
        name: what to write?
        runs-on: ubuntu-latest

        steps:
             - name: step1
               run: |
                    echo "${{ github.toJSON.event.repsitory}}"



