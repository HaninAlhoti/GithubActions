name: output github context info.

on: workflow_dispatch

jobs:
    info:
        name: what to write?
        runs-on: ubuntu-latest

        steps:
             - name: step1
               run: |
                    echo "${{ toJSON(github.event) }}"
